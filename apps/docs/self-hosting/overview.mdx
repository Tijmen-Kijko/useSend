---
title: Self hosting useSend
description: "An end-to-end guide on how to self-host useSend. An open-source sending infrastructure for developers."
---

## Prerequisites

- A [GitHub](https://github.com) account
- An [AWS](https://aws.amazon.com) account

If you have any questions join [#self-host](https://discord.gg/gbsvjb9MqV) on discord.

## Step 1: Environment variables

useSend depends on AWS SES to send emails and SNS to receive email status. Along with that it also depends on Postgres as a database and Redis for queue. Copy the `.env.selfhost.example` file to `.env` and fill in the values.

<Steps>
  <Step title="AWS credentials">
  Create a dedicated IAM principal for useSend. Do not use the AWS root account, administrator credentials, or the broad `AmazonSNSFullAccess` / `AmazonSESFullAccess` managed policies. Grant only the SES, SNS and STS actions useSend actually calls.

Add the following environment variables.

    ```env
    AWS_ACCESS_KEY_ID=<access-key-id>
    AWS_SECRET_ACCESS_KEY=<secret-access-key>
    ```

<Tip>
  Follow the [least-privilege AWS credential guide](/get-started/create-aws-credentials)
  for the exact action list and rotation procedure.
</Tip>

On a non-AWS host such as a VPS, inject the dedicated IAM access key through
your deployment secret store. If useSend runs on AWS, prefer the AWS SDK default
credential chain with a workload role instead of static credentials.

Both `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` must be provided
together. If both are omitted, useSend uses the AWS SDK default credential
provider chain.

  </Step>
  <Step title="GitHub app credentials for login">
  useSend uses GitHub authentication for login.

Use this link to [create an github app](https://docs.github.com/en/apps/creating-github-apps/about-creating-github-apps/about-creating-github-apps)

Callback URL : `https://<your-usesend-instance>/api/auth/callback/github`

Under **Permissions & events**, set **Account permissions → Email addresses**
to **Read-only**. useSend needs the authenticated user's email address to match
new accounts to team invitations.

![github app](/images/github-callback.png)

Add the following environment variables.

```env
GITHUB_ID="<your-github-client-id>"
GITHUB_SECRET="<your-github-client-secret>"

# Recommended for self-hosted production.
ADMIN_EMAIL="<primary-admin-email>"
SELF_HOSTED_ALLOWED_EMAILS="<primary-admin-email>,<second-admin-email>"
```

For self-hosted instances, `ADMIN_EMAIL` controls who may bootstrap an empty
installation. Once the first account exists, all additional accounts still
require a team invitation. When `SELF_HOSTED_ALLOWED_EMAILS` is configured,
both existing and new dashboard users must also be present in that allowlist.
Email matching is case-insensitive.

Use `NEXTAUTH_URL=https://...` in production. useSend enables secure NextAuth
cookies whenever the configured URL uses HTTPS.

<Warning>
Treat the allowlist and your identity-provider access policy as separate
controls. If you place useSend behind an access proxy such as Cloudflare
Access, keep the origin inaccessible except through that trusted proxy.
</Warning>

### Self-hosted access recovery

If an authorized administrator loses dashboard access:

1. Prefer restoring that existing administrator's email in both the access
   proxy policy and `SELF_HOSTED_ALLOWED_EMAILS`, then restart useSend.
2. If the database has no users yet, set `ADMIN_EMAIL` to the approved
   recovery identity, include it in `SELF_HOSTED_ALLOWED_EMAILS`, and sign in
   through the configured OAuth provider.
3. If users already exist but no authorized identity remains, use controlled
   database maintenance access to create a one-time team invitation for an
   approved recovery email, add that email to the allowlist, sign in, and then
   remove the temporary recovery access.
4. If the lockout may be related to credential compromise, rotate the OAuth
   client secret and `NEXTAUTH_SECRET` after access is restored.

  <Info>If you want email/password login, please help us out with the [code](https://github.com/usesend/usesend) </Info>  
  </Step>
  <Step title="Database & Redis">
  useSend uses Postgres as a database and Redis as a queue. You need to create a new database and add the following environment variables.

If you're using docker-compose, it's all automatically done for you.

```env
DATABASE_URL="postgres://<username>:<password>@<host>:<port>/<database-name>"
REDIS_URL="redis://<username>:<password>@<host>:<port>"
```

If you're sharing a Redis instance across multiple apps and using Redis ACL for isolation, set `REDIS_KEY_PREFIX` to namespace all keys (e.g. `REDIS_KEY_PREFIX="usesend"` prefixes all keys with `usesend:`).

  </Step>
  <Step title="Next auth url and secret">
  Url is the app url you're going to use and secret is random string. You can generate a random secret using this command.

```sh
openssl rand -base64 32
```

Add the following environment variables.

```env
  NEXTAUTH_URL="https://<your-usesend-instance>"
  NEXTAUTH_SECRET="<your-usesend-secret>"
```

  </Step>
  <Step title="Email content retention (optional)">
  By default, stored email content stays in the database forever. To bound database growth, set `EMAIL_CLEANUP_DAYS` to a positive number of days. A daily job (midnight UTC) then clears the `text`, `html`, `attachments` and `headers` values for emails older than the cutoff. Delivery status and event history are kept.

```env
EMAIL_CLEANUP_DAYS=30
```

Leave it unset to keep email content indefinitely.

  </Step>
</Steps>

## Step 2: Setting up the app

You can use any platform that supports Docker to host useSend. If you have any questions drop in the [discord channel](https://discord.gg/gbsvjb9MqV) and we'll try to help you out.

### Docker

Follow this guide to setup your docker instance: [Set up docker](/get-started/set-up-docker)

[![Docker image](https://img.shields.io/badge/dockerhub-images-important.svg?logo=Docker)](https://hub.docker.com/r/usesend/usesend)

## Step 3: Setting up a region

In order to send emails, you need to select a region in AWS. Use a region where your users are located / where useSend is hosted. If you're confused just use `us-east-1`.

You can check available regions [here](https://docs.aws.amazon.com/general/latest/gr/ses.html)

Once you log in to useSend, it will prompt you add SES configuration.

- Add the region
- Add the callback url, which is basically the app url. Note this should be accesible from internet. This is how you get the delivery status of the emails.
- You don't need to update the send rate, it's automatically based on your account.

![github app](/images/ses-settings/add-ses-settings.png)

## Step 4: Getting out of sandbox mode

<Warning>Don't forget to get the SES account out of sandbox mode.</Warning>

![github app](/images/ses-settings/sandbox.png)

## Step 5: SMTP Proxy Server (Optional)

The SMTP proxy server is an optional component that allows applications to send emails through useSend using standard SMTP protocol instead of the REST API. This is useful for legacy applications, email clients, or any software that needs to send emails via SMTP.

<Tip>
  The complete source code for the SMTP proxy server is available at:
  [usesend/usesend/tree/main/apps/smtp-server](https://github.com/usesend/usesend/tree/main/apps/smtp-server)
</Tip>

### When to use the SMTP proxy:

- **Legacy applications** that only support SMTP
- **Email clients** like Thunderbird, Outlook, Apple Mail
- **Applications** that can't easily integrate with REST APIs
- **Third-party software** that requires SMTP configuration

### Docker Compose Setup

Create a `docker-compose.yml` file for the SMTP server:

```yaml
name: usesend-smtp-server

services:
  smtp-server:
    container_name: usesend-smtp-server
    image: usesend/smtp-proxy:latest
    environment:
      SMTP_AUTH_USERNAME: "usesend" # Username for SMTP authentication
      USESEND_BASE_URL: "https://your-usesend-instance.com" # Your useSend instance URL

      # Optional: SSL certificate paths for secure connections
      # USESEND_API_KEY_PATH: "/certs/server.key"
      # USESEND_API_CERT_PATH: "/certs/server.crt"

    # Optional: Mount SSL certificates
    # volumes:
    #   - ./certs/server.key:/certs/server.key:ro
    #   - ./certs/server.crt:/certs/server.crt:ro

    ports:
      - "25:25" # Standard SMTP
      - "587:587" # SMTP with STARTTLS
      - "2587:2587" # Alternative SMTP port
      - "465:465" # SMTP over SSL/TLS
      - "2465:2465" # Alternative SMTPS port

    restart: unless-stopped
```

Run the SMTP server:

```bash
docker-compose up -d
```

### SMTP Configuration

To send emails through the proxy, configure your application with these SMTP settings:

- **Host**: Your server's IP address or domain
- **Ports**: 25, 587 (STARTTLS), 465 (SSL/TLS), 2587, or 2465
- **Username**: `usesend` (or your custom `SMTP_AUTH_USERNAME`)
- **Password**: Your useSend API key
- **Encryption**: STARTTLS (ports 25, 587, 2587) or SSL/TLS (ports 465, 2465)

<Tip>
  The SMTP proxy forwards all emails to your useSend instance, so make sure your
  main useSend application is running and accessible.
</Tip>

<Warning>
  Ensure your firewall allows traffic on the SMTP ports you're using. For
  production deployments, consider using non-standard ports (2587, 2465) to
  avoid conflicts.
</Warning>

## Credential lifecycle

Treat the useSend platform credentials and each application's API key as
separate security boundaries.

### Project API keys

- Create one API key per project and environment. Do not share one global key
  across applications.
- Use `SENDING` for normal application keys. Use `FULL` only for trusted
  administration automation that genuinely needs domains, contacts, campaigns,
  or analytics APIs.
- Scope a project key to one sending domain whenever practical.
- Store the key in the application's secret store; never commit it to Git or
  bake it into a client-side bundle.

To rotate a project key without downtime:

1. Create a replacement `SENDING` key with the same intended domain scope.
2. Update the project's secret store and deploy/restart that project.
3. Send a controlled test email and verify it appears under the replacement
   key.
4. Delete the old key in useSend.
5. Watch application errors and useSend send failures during the observation
   window.

If a project key is exposed, revoke only that project's key first. Other
project keys, AWS credentials, and dashboard credentials do not need to change
unless there is evidence they were exposed too.

### Dashboard and OAuth secrets

`NEXTAUTH_SECRET` and OAuth client secrets belong to the platform deployment,
not to individual projects. Keep them in the deployment secret store.

- Rotating `NEXTAUTH_SECRET` invalidates existing sessions, so plan a
  controlled restart and expect administrators to sign in again.
- Rotate the GitHub/Google OAuth client secret at the identity provider, deploy
  the replacement, verify login, then revoke the old secret.
- Keep `ADMIN_EMAIL` and `SELF_HOSTED_ALLOWED_EMAILS` as configuration, not
  credentials. Changes to them should still be reviewed because they change
  who can access the dashboard.

### AWS credentials

Use a dedicated least-privilege IAM identity as described in the
[AWS credential guide](/get-started/create-aws-credentials). AWS credentials
must never be reused as project API keys.

## Next steps

You're all set up now.

- Setup a domain.
- Create an API key.
- Start sending emails.

If you have any questions, please join [#self-host](https://discord.gg/gbsvjb9MqV) on discord.

<Tip>
  A community member shared a short write-up on hosting useSend with
  [Coolify](https://mattstein.com/thoughts/coolify-unsend/). Give it a read if
  you need another reference.
</Tip>
